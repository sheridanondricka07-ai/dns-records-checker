{
  "name": "dns-records-checker",
  "version": "1.0.0",
  "description": "High-performance tool to check DNS records for multiple domains.",
  "main": "api/check-records.js",
  "scripts": {
    "start": "vercel dev",
    "deploy": "vercel"
  },
  "dependencies": {
    "p-limit": "^3.1.0"
  },
  "devDependencies": {
    "vercel": "^32.0.0"
  }
}
